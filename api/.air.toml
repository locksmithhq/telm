[build]
  cmd = "go build -o ./dev/main ./cmd"
  bin = "./dev/main"
  full_bin = "./dev/main serve"
  include_ext = ["go"]
  exclude_dir = ["dev", "vendor"]
  delay = 1000

[log]
  time = false

[color]
  main = "magenta"
  watcher = "cyan"
  build = "yellow"
  runner = "green"
